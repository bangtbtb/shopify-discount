import { DateTime, DiscountStatus } from "@shopify/discount-app-components";

// export function getDiscountStatus(status: DiscountStatus, endAt: DateTime) {
//     if (status === "ACTIVE") {
//         return A
//     }
// }
