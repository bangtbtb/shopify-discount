{%- assign col_ids = product.collections | map: 'id' -%}

<style>
  .vd_table {
    border: solid 1px green;
    border-radius: 4px;
    text-align: center;
    padding: 0;
    border-spacing: 0;
  }

  .vd_table thead {
    margin: 0;
    background-color: green;
    border-bottom: solid 1px green;
  }
</style>

<div id="volume_112dj2abbdf" product="{{ product.id }}" cols="{{ col_ids | join: "," }}">
  <p>Volume discount</p>
</div>

<script src="{{ "load_vd.js" | asset_url }}" defer></script>

{% schema %}
{
  "name": "Volume Discount",
  "target": "section",
  "settings": [{ "type": "product", "id": "product", "label": "product", "autofill": true }]
}
{% endschema %}
